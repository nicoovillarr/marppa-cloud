"use client";

import "@/dashboard/orbit/orbitFactory";
import "@/dashboard/mesh/meshFactory";

const OrbitLayout = ({ children }: { children: React.ReactNode }) => children;
export default OrbitLayout;
