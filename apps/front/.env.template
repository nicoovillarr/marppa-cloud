NEXT_PUBLIC_API_URL=""

# LAN/public address of the host running cloud-scripts (the one fibers DNAT to,
# e.g. 192.168.1.100). Used to render exact SSH/connect commands in the UI.
# Leave empty to fall back to the address the UI is served from.
NEXT_PUBLIC_HOST_ADDRESS=""

# Cloudflare Turnstile site key (captcha on login / password reset). Must pair
# with TURNSTILE_SECRET on the back-end. Leave empty in dev to hide the widget.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=""

# WebSocket URL for live resource updates (cloud-scripts WS server).
# Use wss:// in production. e.g. ws://localhost:<WS_PORT> in dev.
NEXT_PUBLIC_WS_URL=""