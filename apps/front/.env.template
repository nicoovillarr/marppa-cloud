NEXT_PUBLIC_API_URL=""

# LAN/public address of the host running cloud-scripts (the one fibers DNAT to,
# e.g. 192.168.1.100). Used to render exact SSH/connect commands in the UI.
# Leave empty to fall back to the address the UI is served from.
NEXT_PUBLIC_HOST_ADDRESS=""