ws.cloud.marppa.com {
	@ws {
		header Connection *Upgrade*
		header Upgrade websocket
	}
	handle @ws {
		reverse_proxy 127.0.0.1:8080
	}
	handle {
		respond "Upgrade Required" 426
	}
}

host.cloud.marppa.com {
	redir https://cloud.marppa.com{uri}
}

import sites/*.caddy
